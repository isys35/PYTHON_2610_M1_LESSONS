[tool.poetry]
name = "example2"
version = "0.1.0"
description = ""
authors = ["Dzmitry"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
